PROJECT(os3)
CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
AUX_SOURCE_DIRECTORY(. DIR_SRCS)

SET(CMAKE_C_FLAGS "${CMAKE_CFLAGS} -g -Wall -Wno-unused-variable -rdynamic")

ADD_LIBRARY(os3common STATIC ${DIR_SRCS})
